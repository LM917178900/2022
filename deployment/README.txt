################################
# 参数说明
################################

# 部署前执行的命令
STEP_BEFORE="echo STEP_BEFORE"

# 部署完成后执行的命令
STEP_AFTER="echo STEP_AFTER"

# 需要保留的文件
KEEPFILE="one/config.php"

# 不需要备份的文件
BACKUP_EXCLUDE_FILE="three *.svn *.zip *.tar *.tar.gz"

# 关闭服务的命令
CMD_SHUTDOWN="echo shutdown"

# 启动服务的命令
CMD_STARTUP="echo startup"



